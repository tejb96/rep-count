todo

tasks 
-make a start session/ stop session button in frontend
-rename start/stop movenet buttons to start set/stop set
-check rendering of detector in tracker and revise that
-further test and improve rep counting logic
-train a mediapipe or tensorflow model to detect poses: https://chuoling.github.io/mediapipe/solutions/pose_classification.html
        Data Collection:

            Collect videos or images of people doing push-ups and sit-ups.

            Label the data with phases (e.g., "up", "down").

        Model Training:

            Use a framework like TensorFlow.js or MediaPipe to train a pose classification model.

            Export the trained model for use in your React app.

        Integration:

            Load the model in your app and use it to classify the user's pose in real-time.

            Count reps based on transitions between classified poses.






done👍
-make a paper or card comp
-make standard component that each page can import and it handles theme change
-front end axios config set correctly to use credentials
-show user name and avatar on navbar after login
	-verify that middleware is correctly extractinga nd validating the jwt from cookie
	-ensure that the /api/users/me route is correctly returning user data
	-test api/users/me using postmant with a cookie
	-backend redirects to frontend home, but loginpage in frontend might not be configured to handle that
	
	
	




